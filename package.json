{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "minify": "terser zimsy.js -o zimsy.min.js && csso zimsy.css -o zimsy.min.css"
  },
  "dependencies": {
    "csso-cli": "^4.0.2",
    "terser": "^5.43.1"
  }
}